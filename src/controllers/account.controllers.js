const account = require ('../models/account.model')
const createaccount = (req,res)=>{
    try{
        console.log("test",req.body);
        
    }
    catch(e){

    }
}
module.exports={
    createaccount
}